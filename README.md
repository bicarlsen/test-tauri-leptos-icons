# `leptos-icons` + `tauri` test 

## Use
```sh
cargo tauri dev
```

Expects local versions of `leptos` and `leptos-icons`. 
Be aware that some paths in `Cargo.toml` must be setup correctly.
See [`Cargo.toml`](./Cargo.toml) for details.